spring:
  datasource:
    url: jdbc:h2:mem:homework;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  flyway:
    enabled: true

app:
  jwt:
    issuer: homework-analysis-test
    secret: test-secret-test-secret-test-secret-1234
    access-minutes: 30
