"""内部 OCR 服务。

两层刻意分开：

- ``contracts``：与 Java 侧共享的 JSON 契约，形状一旦发布就不能改（要改就换 schemaVersion）；
- ``pipeline``：预处理、版面检测、识别与归一化的具体实现，可以随模型升级整体替换。

本服务不连接业务数据库，也不持有对象存储凭据：它只接收页面图像、返回候选区域。
OCR 结果在任何情况下都只是候选数据，正式题目与答案只能由教师在 Java 侧确认后写入。
"""
