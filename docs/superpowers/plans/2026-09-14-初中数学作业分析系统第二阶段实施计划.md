# 初中数学作业分析系统第二阶段实施计划

> **执行要求：** 实施本计划时使用 `superpowers:executing-plans` 按任务顺序推进；每个任务遵循“失败测试—最小实现—针对性验证—全量回归—差异审查”的闭环。未经用户明确授权，不执行提交、推送、部署、持久化数据库迁移或真实模型调用。

**目标：** 在保留现有核心教学闭环的基础上，建立稳定的自动化验证基线，补齐真实集成证据、分层练习 Word 导出和论文离线评测，使项目达到可演示、可答辩、可量化评价的毕业设计交付状态。

**架构：** 继续采用 Vue 单页应用和 Spring Boot 模块化单体。规则评分、画像统计和分层选题保持确定性；大模型仍通过后端可替换适配器提供建议；教师确认是写入正式成绩和生成教学材料的唯一入口。新增能力按测试基础设施、外部边界、教学功能和论文实验四类分开实施。

**技术栈：** Java 21、Spring Boot 4.1.1、Spring Security、Spring JDBC、Flyway、MySQL 8.4、H2、Apache POI、Vue 3.5.42、TypeScript 6.0.3、Vite 8.3.0、Element Plus、ECharts、JUnit、MockMvc、WireMock、Testcontainers、Vitest、Playwright、Python 3 标准库。

## 全局约束

- 项目目录固定为 `E:\0_all_private_project\homework_for_student_system`；
- 所有 Markdown 文档使用中文，代码标识、协议字段和命令可保留英文；
- 产品只面向初中数学教师，不新增学生端、家长端、在线考试、OCR、RAG、向量数据库和多学科能力；
- 模型建议必须经教师确认，未确认结果不得进入画像、错因统计和分层练习；
- 模型密钥只保存在后端环境变量中，任何请求、响应、日志和测试快照都不得包含真实密钥；
- 模型输入不得包含学生姓名、学号、班级名称、登录令牌和其他身份信息；
- 正式持久化数据库仍为 MySQL 8.4，结构修改必须新增 Flyway 迁移，不改写已经存在的 V1 至 V5；
- 一次性 H2 和 Testcontainers 数据库可以用于自动化测试；持久化 MySQL、真实模型、部署和远程状态变更必须单独获得授权；
- 不新增微服务、消息队列、向量数据库或独立 Python 在线服务；
- 每个任务结束时运行针对性测试并检查改动范围，不自动执行 Git 提交。

## 当前基线

- 已实现登录、数据隔离、班级学生、知识点题库、作业导入、规则评分、AI 建议、教师复核和学情分析；
- `ClassroomApiTest` 4 项针对性测试已通过；
- 后端全量测试当前有 11 项外键清理错误，根因是测试数据库夹具不统一；
- 前端类型检查和生产构建通过；
- Vitest 当前错误收集 `frontend/e2e` 下的 Playwright 文件；
- 生产构建存在约 947 kB 和 1,121 kB 的大分包警告；
- 真实 MySQL、真实模型、分层练习和论文评测尚无完成证据。

---

## 任务一：统一后端集成测试数据库隔离

**优先级：** P0

**文件：**

- 新建：`backend/src/test/java/com/homework/analysis/testing/TestDatabaseCleaner.java`
- 修改：`backend/src/test/java/com/homework/analysis/analytics/AnalyticsReviewApiTest.java`
- 修改：`backend/src/test/java/com/homework/analysis/assignment/AssignmentImportApiTest.java`
- 修改：`backend/src/test/java/com/homework/analysis/auth/AuthApiTest.java`
- 修改：`backend/src/test/java/com/homework/analysis/classroom/ClassroomApiTest.java`
- 修改：`backend/src/test/java/com/homework/analysis/classroom/StudentApiTest.java`
- 修改：`backend/src/test/java/com/homework/analysis/question/QuestionApiTest.java`

**接口：**

- 输入：Spring 提供的 `JdbcTemplate`；
- 输出：`TestDatabaseCleaner.clean(JdbcTemplate jdbc)`，按完整外键顺序清空业务表；
- 约束：不删除 `flyway_schema_history`，不关闭数据库外键约束，不让某个测试依赖执行顺序。

- [x] **步骤 1：保留当前全量失败作为回归证据**

运行：

```powershell
mvn -f backend/pom.xml -q test
```

预期：失败信息包含 `fk_aq_question`、`fk_grading_answer` 或 `fk_class_teacher`，证明根因是测试数据残留。

- [x] **步骤 2：建立唯一测试清理器**

新增以下职责明确的测试工具：

```java
package com.homework.analysis.testing;

import org.springframework.jdbc.core.JdbcTemplate;

public final class TestDatabaseCleaner {
    private static final String[] TABLES = {
        "grading_audit", "teacher_review", "grading_result", "ai_grading_task",
        "student_answer", "submission", "assignment_question", "assignment",
        "rubric_item", "question_knowledge_point", "question", "knowledge_point",
        "student", "school_class", "teacher", "app_user"
    };

    private TestDatabaseCleaner() {}

    public static void clean(JdbcTemplate jdbc) {
        for (String table : TABLES) {
            jdbc.update("delete from " + table);
        }
    }
}
```

表名来自代码内固定允许列表，不接收外部输入。

- [x] **步骤 3：替换每个集成测试的局部清理 SQL**

每个 `@BeforeEach` 的第一行统一为：

```java
TestDatabaseCleaner.clean(jdbc);
```

删除各测试类重复的 `delete from` 语句，只保留本测试所需的种子数据。

- [x] **步骤 4：验证单类和跨类运行都稳定**

```powershell
mvn -f backend/pom.xml -q -Dtest=ClassroomApiTest,QuestionApiTest test
mvn -f backend/pom.xml -q test
```

验收标准：单类、组合和全量测试全部通过；重复运行全量测试两次仍通过；测试数量不少于 24 项。

- [x] **步骤 5：审查测试隔离结果**

检查没有使用 `SET REFERENTIAL_INTEGRITY FALSE`、没有删除迁移历史，也没有通过测试排序规避问题。

---

## 任务二：隔离 Vitest 单元测试与 Playwright 端到端测试

**优先级：** P0

**文件：**

- 修改：`frontend/vite.config.ts`
- 修改：`frontend/package.json`
- 修改：`frontend/src/App.test.ts`
- 新建：`frontend/src/api/client.test.ts`
- 修改：`README.md`

**接口：**

- `npm run test` 只运行 `frontend/src/**/*.test.ts`；
- `npm run test:e2e` 只运行 `frontend/e2e/**/*.spec.ts`；
- 两套测试可以独立失败和独立报告。

- [x] **步骤 1：确认收集范围冲突**

```powershell
npm --prefix frontend run test
```

预期：Vitest 报告 `Playwright Test did not expect test() to be called here`。

- [x] **步骤 2：固定 Vitest 收集范围**

将配置改为：

```ts
test: {
  environment: 'jsdom',
  globals: true,
  css: true,
  include: ['src/**/*.test.ts'],
},
```

将脚本固定为一次性运行：

```json
"test": "vitest run"
```

- [x] **步骤 3：补充 API 错误边界单元测试**

在 `client.test.ts` 中使用 Axios 适配器或 MockAdapter 等项目已有能力验证：

```ts
it('收到 401 时清除登录状态并保留服务端错误码', async () => {
  // 构造 error.response.status = 401 和 error.response.data.error.code = 'AUTH_REQUIRED'
  // 断言令牌被移除，展示消息不是内部堆栈。
})
```

若不引入新依赖，抽取 `normalizeApiError(error: unknown): ApiError` 为纯函数直接测试。

- [x] **步骤 4：更新中文验证命令**

README 中统一使用：

```powershell
npm --prefix frontend run test
npm --prefix frontend run typecheck
npm --prefix frontend run build
```

- [x] **步骤 5：运行前端静态验证**

```powershell
npm --prefix frontend run test
npm --prefix frontend run typecheck
npm --prefix frontend run build
```

验收标准：Vitest 不再加载 `e2e` 文件；单元测试、类型检查和构建全部退出码为 0。

---

## 任务三：补齐大模型适配器契约与失败路径测试

**优先级：** P1

**文件：**

- 新建：`backend/src/test/java/com/homework/analysis/grading/ai/OpenAiCompatibleModelClientTest.java`
- 新建：`backend/src/test/java/com/homework/analysis/grading/ai/AiGradingTaskWorkerTest.java`
- 修改：`backend/src/main/java/com/homework/analysis/grading/ai/OpenAiCompatibleModelClient.java`
- 修改：`backend/src/main/java/com/homework/analysis/grading/ai/AiGradingTaskWorker.java`
- 修改：`backend/src/main/java/com/homework/analysis/grading/ai/ModelProperties.java`
- 修改：`backend/src/main/resources/application.yml`

**接口：**

- 继续实现 `AiModelClient.grade(AiGradingRequest): AiGradingSuggestion`；
- HTTP 路径为可配置的 `app.model.api-path`；
- 任务状态只允许 `PENDING → RUNNING → SUCCEEDED`、`RUNNING → RETRYABLE_FAILED` 或 `RUNNING → MANUAL_REQUIRED`。

- [x] **步骤 1：用 WireMock 编写成功契约测试**

测试服务返回：

```json
{
  "output": [{
    "content": [{
      "type": "output_text",
      "text": "{\"suggestedScore\":6,\"scoreDetails\":[{\"rubricId\":1,\"score\":6,\"evidence\":\"列式正确\"}],\"errorType\":\"CORRECT\",\"teacherExplanation\":\"过程完整\",\"studentFeedback\":\"保持书写规范\",\"needsTeacherReview\":true}"
    }]
  }]
}
```

断言请求方法、路径、Bearer 头、`store=false`、`text.format.type=json_schema` 和模型名称正确。

- [x] **步骤 2：验证脱敏边界**

请求体断言不能出现种子字符串 `张三`、`20260001`、`七年级一班` 和 `Bearer`；只允许出现 `answer-<数据库编号>` 形式的匿名标识。

- [x] **步骤 3：覆盖异常响应**

分别为 HTTP 429、500、读取超时、缺少 `output_text`、非法 JSON、分项合计不等于总分、未知错因标签编写失败测试。适配器统一抛出稳定领域错误，不把响应体和授权头拼进异常消息。

- [x] **步骤 4：让任务领取具备条件更新保护**

将领取 SQL 改为带旧状态条件的更新：

```sql
update ai_grading_task
set status = 'RUNNING', updated_at = current_timestamp(3)
where id = :id
  and status in ('PENDING', 'RETRYABLE_FAILED')
  and (next_attempt_at is null or next_attempt_at <= current_timestamp(3))
```

只有受影响行数为 1 的调用者可以继续请求模型；受影响行数为 0 时重新读取一个任务或返回 `EMPTY`，防止两个线程重复处理同一答案。

- [x] **步骤 5：验证有限重试状态**

使用固定时钟或可注入 `Clock` 断言第 1、2、3 次失败的等待时间分别为 30 秒、2 分钟、10 分钟，第 4 次进入 `MANUAL_REQUIRED`，且学生答案始终保留。

- [x] **步骤 6：运行 AI 模块测试**

```powershell
mvn -f backend/pom.xml -q -Dtest=OpenAiCompatibleModelClientTest,AiGradingTaskWorkerTest,AiResultValidatorTest test
```

验收标准：全程只访问 WireMock 本地服务，不调用公网，不需要真实模型密钥。

---

## 任务四：建立一次性 MySQL 8.4 集成验证

**优先级：** P1

**文件：**

- 修改：`backend/pom.xml`
- 新建：`backend/src/test/java/com/homework/analysis/integration/MySqlMigrationIT.java`
- 新建：`backend/src/test/java/com/homework/analysis/integration/MySqlCoreWorkflowIT.java`
- 修改：`README.md`

**接口：**

- 使用 Testcontainers 创建一次性 `mysql:8.4` 容器；
- Flyway 自动执行 V1 至当前最新迁移；
- 测试退出后容器自动销毁，不使用 `compose.yaml` 的 `homework_mysql_data` 卷。

- [x] **步骤 1：增加独立集成测试配置**

在 Maven 中增加 `mysql-it` 配置，Failsafe 只收集 `*IT.java`：

```xml
<includes>
    <include>**/*IT.java</include>
</includes>
```

普通 `mvn test` 不要求本机 Docker；`mvn verify -Pmysql-it` 才执行 MySQL 集成测试。

- [x] **步骤 2：验证迁移完整性**

`MySqlMigrationIT` 使用：

```java
@Testcontainers
@SpringBootTest
class MySqlMigrationIT {
    @Container
    @ServiceConnection
    static MySQLContainer<?> mysql = new MySQLContainer<>("mysql:8.4");
}
```

断言 `flyway_schema_history` 最新成功版本与迁移目录一致，并确认所有核心表存在。

- [x] **步骤 3：验证核心数据库工作流**

通过真实 HTTP 或服务入口执行：创建班级 → 创建学生 → 创建知识点和题目 → 创建作业 → 导入答案 → 运行客观题评分 → 教师确认 → 查询画像。

断言画像只包含已确认结果，并验证唯一约束、时间精度和 JSON 文本在 MySQL 上无方言差异。

- [x] **步骤 4：运行一次性 MySQL 验证**

```powershell
mvn -f backend/pom.xml verify -Pmysql-it
```

执行前只需确认 Docker 可用；不得改用持久化 Compose 数据库规避失败。

- [x] **步骤 5：记录环境证据**

在 README 中记录命令、一次性容器性质和失败排查方式。验收标准是迁移和核心工作流全部通过，测试结束后没有残留业务容器或持久化卷。

---

## 任务五：实现基于已确认画像的分层练习与 Word 导出

**优先级：** P1

**文件：**

- 新建：`backend/src/main/resources/db/migration/V6__tiered_exercises.sql`
- 修改：`backend/src/main/java/com/homework/analysis/question/QuestionCommand.java`
- 修改：`backend/src/main/java/com/homework/analysis/question/QuestionService.java`
- 新建：`backend/src/main/java/com/homework/analysis/exercise/ExerciseTier.java`
- 新建：`backend/src/main/java/com/homework/analysis/exercise/ExerciseSetView.java`
- 新建：`backend/src/main/java/com/homework/analysis/exercise/ExerciseGenerationCommand.java`
- 新建：`backend/src/main/java/com/homework/analysis/exercise/ExerciseRepository.java`
- 新建：`backend/src/main/java/com/homework/analysis/exercise/ExerciseService.java`
- 新建：`backend/src/main/java/com/homework/analysis/exercise/ExerciseDocumentExporter.java`
- 新建：`backend/src/main/java/com/homework/analysis/exercise/ExerciseController.java`
- 新建：`backend/src/test/java/com/homework/analysis/exercise/ExerciseServiceTest.java`
- 新建：`backend/src/test/java/com/homework/analysis/exercise/ExerciseApiTest.java`
- 新建：`backend/src/test/java/com/homework/analysis/exercise/ExerciseDocumentExporterTest.java`

**接口：**

- `POST /api/exercises`：根据班级和来源作业生成草稿；
- `GET /api/exercises?classId=<id>`：列出当前教师的练习单；
- `GET /api/exercises/{exerciseId}`：查看三级题目；
- `POST /api/exercises/{exerciseId}/approve`：教师确认练习单；
- `GET /api/exercises/{exerciseId}/export.docx`：只导出已确认练习单。

- [x] **步骤 1：先编写迁移和领域约束测试**

V6 增加题目难度与练习表：

```sql
alter table question add column difficulty varchar(16) not null default 'MEDIUM';

create table exercise_set (
    id bigint auto_increment primary key,
    teacher_id bigint not null,
    class_id bigint not null,
    source_assignment_id bigint not null,
    title varchar(128) not null,
    status varchar(16) not null default 'DRAFT',
    approved_at timestamp(3),
    created_at timestamp(3) not null default current_timestamp(3),
    updated_at timestamp(3) not null default current_timestamp(3),
    constraint fk_exercise_teacher foreign key (teacher_id) references teacher(id),
    constraint fk_exercise_class foreign key (class_id) references school_class(id),
    constraint fk_exercise_assignment foreign key (source_assignment_id) references assignment(id)
);

create table exercise_item (
    exercise_set_id bigint not null,
    question_id bigint not null,
    tier varchar(16) not null,
    sort_order int not null,
    primary key (exercise_set_id, question_id),
    constraint uk_exercise_tier_order unique (exercise_set_id, tier, sort_order),
    constraint fk_exercise_item_set foreign key (exercise_set_id) references exercise_set(id),
    constraint fk_exercise_item_question foreign key (question_id) references question(id)
);
```

难度只允许 `BASIC`、`MEDIUM`、`ADVANCED`，层级只允许 `FOUNDATION`、`CORRECTION`、`IMPROVEMENT`。

- [x] **步骤 2：实现确定性分层选题**

只读取来源作业的已确认画像：

- 掌握度低于 0.60 的知识点归入基础巩固层；
- 掌握度 0.60 至 0.80 的知识点归入方法纠错层；
- 掌握度高于 0.80 的知识点归入综合提升层；
- 每层目标 5 题，只出主知识点落在本层的题，先按该层的难度偏好、再按题目编号稳定排序；
- 同一练习单不得重复题目；题库不足时返回实际数量和中文提示，不调用模型编造题目。

> **实施决策（2026-09-15）**：原措辞"优先加入"未说明本层题量不足时怎么办。
> 实现时定为**不跨层借题**——本层知识点只有几道题就出几道，不用别层的题补满。
> 理由是巩固层要给最薄弱的学生降难度，用提升层的题补足会让三层实际难度趋同，
> 分层名存实亡；题量不齐只是排版问题，用 `notices` 说明即可。
> 各层目标集合天然互斥（`tierOf` 是函数，一个知识点只属于一层；每道题只有一个主知识点），
> 因此一层不可能抢走另一层的题，选题时无需去重。

核心签名固定为：

```java
ExerciseSetView generate(long teacherId, ExerciseGenerationCommand command);
ExerciseSetView approve(long teacherId, long exerciseId);
byte[] exportDocx(long teacherId, long exerciseId);
```

- [x] **步骤 3：保证教师数据隔离和状态机**

生成时验证班级与来源作业属于同一教师且相互匹配；无已确认评分时返回 `EXERCISE_NO_CONFIRMED_RESULTS`；草稿才能修改，只有 `APPROVED` 状态可以导出。

- [x] **步骤 4：生成可打印 Word 文档**

使用 `XWPFDocument` 输出：标题、班级、来源作业、生成时间、三个层级说明、题号、题干、分值和知识点；答案与评分细则放在文档末尾的教师参考区。

HTTP 响应固定为：

```http
Content-Type: application/vnd.openxmlformats-officedocument.wordprocessingml.document
Content-Disposition: attachment; filename="exercise-<id>.docx"
```

- [x] **步骤 5：测试文档和业务规则**

测试至少覆盖无确认结果、跨教师访问、题库不足、稳定排序、重复题过滤、草稿禁止导出、确认后导出。导出测试重新用 POI 打开字节数组，断言标题、三级标题和教师参考区存在。

- [x] **步骤 6：运行后端练习模块测试**

```powershell
mvn -f backend/pom.xml -q -Dtest=ExerciseServiceTest,ExerciseApiTest,ExerciseDocumentExporterTest test
mvn -f backend/pom.xml -q test
```

---

## 任务六：补充分层练习教师端页面

**优先级：** P1

**文件：**

- 修改：`frontend/src/api/types.ts`
- 修改：`frontend/src/api/client.ts`
- 修改：`frontend/src/router/index.ts`
- 修改：`frontend/src/layouts/AppLayout.vue`
- 新建：`frontend/src/views/ExerciseView.vue`
- 新建：`frontend/src/views/ExerciseView.test.ts`
- 修改：`frontend/e2e/teacher-workflow.spec.ts`

**接口：**

- 页面路径：`/exercises`；
- 复用后端五个练习接口；
- 下载必须携带 JWT 并把 Blob 保存为 `.docx`，不能直接在 URL 中附带令牌。

- [x] **步骤 1：定义前端类型和 API 方法**

```ts
export type ExerciseTier = 'FOUNDATION' | 'CORRECTION' | 'IMPROVEMENT'
export type ExerciseStatus = 'DRAFT' | 'APPROVED'

export interface ExerciseSet {
  id: number
  classId: number
  sourceAssignmentId: number
  title: string
  status: ExerciseStatus
  items: ExerciseItem[]
}
```

实现 `listExercises`、`generateExercise`、`approveExercise` 和 `downloadExerciseDocx`。

- [x] **步骤 2：先写页面行为测试**

测试加载态、空状态、生成失败、三级题目展示、确认按钮禁用状态和已确认后的下载按钮。

- [x] **步骤 3：实现教师工作流**

页面由“来源班级与作业选择”“三级预览”“确认并导出”三个区域组成。继续使用现有方格批改簿设计语言，不新增另一套视觉系统。

- [x] **步骤 4：加入路由和导航**

新增受保护路由：

```ts
{ path: 'exercises', name: 'exercises', component: () => import('../views/ExerciseView.vue') }
```

- [x] **步骤 5：扩展浏览器主流程**

Playwright 路径扩展为：登录 → 学情画像 → 生成分层练习 → 查看三级题目 → 教师确认 → 下载 Word。下载测试断言建议文件名以 `.docx` 结尾且文件非空。

- [x] **步骤 6：运行前端验证**

```powershell
npm --prefix frontend run test
npm --prefix frontend run typecheck
npm --prefix frontend run build
npm --prefix frontend run test:e2e
```

---

## 任务七：建立论文离线评测数据与指标脚本

**优先级：** P1

**文件：**

- 新建：`evaluation/README.md`
- 新建：`evaluation/evaluate_grading.py`
- 新建：`evaluation/tests/test_evaluate_grading.py`
- 新建：`evaluation/data/grading_cases.example.csv`
- 新建：`evaluation/data/error_labels.example.csv`
- 修改：`.gitignore`
- 新建：`docs/experiments/2026-09-14-实验记录模板.md`

**接口：**

- 输入 CSV 只使用匿名样本编号；
- 输出 JSON 包含评分误差、容差命中率、错因分类指标、教师修改率、平均耗时和样本数；
- 脚本只使用 Python 标准库，避免引入独立在线服务。

- [x] **步骤 1：定义匿名数据格式**

`grading_cases.example.csv` 表头固定为：

```text
case_id,total_score,teacher_score,ai_score,teacher_error_type,ai_error_type,teacher_modified,teacher_seconds,ai_seconds,input_tokens,output_tokens
```

示例数据必须是虚构数据，不包含姓名、学号和真实学校。

- [x] **步骤 2：先写指标单元测试**

覆盖以下确定性样本：

```python
def test_score_metrics():
    pairs = [(8, 8), (6, 7), (5, 3)]
    assert mean_absolute_error(pairs) == 1.0
    assert within_tolerance_rate(pairs, tolerance=1) == 2 / 3
```

同时覆盖零样本、未知错因标签、除零保护和混淆矩阵。

- [x] **步骤 3：实现论文核心指标**

脚本输出：

- 平均绝对误差 `MAE`；
- 评分完全一致率；
- 误差不超过 1 分的命中率；
- 各错因标签的精确率、召回率和 F1；
- 宏平均 F1；
- 教师修改率；
- 教师平均用时、AI 平均用时和估算节省比例；
- 输入、输出令牌总量，仅在 CSV 提供数据时计算。

- [x] **步骤 4：生成机器可读结果**

命令固定为：

```powershell
python evaluation/evaluate_grading.py `
  --grading evaluation/data/grading_cases.example.csv `
  --output evaluation/output/metrics.json
```

`evaluation/output/` 加入 `.gitignore`；论文引用数据时把最终匿名数据和结果另行归档，不把真实学生信息提交到仓库。

- [x] **步骤 5：编写中文实验记录模板**

模板包含研究问题、样本来源与脱敏方式、人工评分流程、模型与提示词版本、参数、指标、结果、威胁与局限，不预填虚假结论和效果数字。

- [x] **步骤 6：运行离线评测测试**

```powershell
python -m unittest discover -s evaluation/tests -v
python evaluation/evaluate_grading.py --grading evaluation/data/grading_cases.example.csv --output evaluation/output/metrics.json
```

验收标准：单元测试通过，结果 JSON 可重复生成，示例输出与手算一致。

---

## 任务八：完成并发、性能、安全和最终验收

**优先级：** P2

**文件：**

- 修改：`backend/src/main/java/com/homework/analysis/assignment/AssignmentService.java`
- 修改：`backend/src/main/java/com/homework/analysis/classroom/ClassroomRepository.java`
- 修改：`frontend/src/main.ts`
- 修改：`frontend/src/views/AnalyticsView.vue`
- 修改：`frontend/vite.config.ts`
- 修改：`README.md`
- 修改：`docs/2026-09-14-项目阶段总结.md`

**接口：** 不改变已有 HTTP 请求和响应字段，只消除竞态、降低前端首屏体积并补齐验收记录。

- [x] **步骤 1：消除新增记录编号查询竞态**

把 `insert` 后按 `max(id)` 或业务字段回查编号的代码改为 Spring JDBC `GeneratedKeyHolder`，并增加并发创建测试，确保两个请求不会拿到同一编号。

> **实施记录（2026-09-15）**：`max(id)` 回查只有 `AssignmentService` 一处，已在里程碑一改完。
> 本轮把余下四处"插入后按业务字段回查"也改成驱动回传：`QuestionService.create`、
> `KnowledgePointService.create`、`ClassroomRepository.insert`、`StudentRepository.insert`、
> `TeacherReviewService.review`、`SubmissionImportService.findOrCreateSubmission`。
> 这四处原先都**恰好**正确——回查的列上正好有唯一约束——但正确性依赖这个隐式前提，
> 约束一旦调整就会静默取到别人的行。`GeneratedKeys` 因此增加了 `JdbcClient` 具名参数重载，
> 免得只注入了 `JdbcClient` 的服务再注入一个 `JdbcTemplate`。
> 并发测试见 `backend/src/test/java/com/homework/analysis/shared/jdbc/GeneratedKeysConcurrencyTest.java`，
> 覆盖建作业（含子表明细）、建题目（含知识点关联）、建班级三条路径。
> 该测试是**回归护栏而非必现用例**：改成驱动回传后它任何交错下都通过，
> 若有人退回回查写法则只是很可能失败，取决于线程交错。

- [x] **步骤 2：检查所有教师边界查询**

对班级、学生、题目、作业、评分结果和练习单的读取、修改、删除逐一确认 SQL 含 `teacher_id` 或可信拥有关系连接；跨教师访问统一返回 404，不泄露资源存在性。

> **实施记录（2026-09-15）**：逐个端点过了一遍。教师身份全部来自
> `CurrentTeacher.id(authentication)`，没有任何端点从路径或请求体取教师编号。
>
> **发现并修复一处**：`KnowledgePointService.requireActiveOwned` 把"不属于我""不存在"
> "已停用"三种情况压成同一个 400 `KNOWLEDGE_POINT_INVALID`。跨教师访问返回 400 而不是
> 全站统一的 404，两个请求的状态码不同，等于给出一个可探测知识点编号存在性的预言机。
> 现拆成：查不到（不存在或不属于我）→ 404 `KNOWLEDGE_POINT_NOT_FOUND`；
> 查得到但 `active = false` → 400 `KNOWLEDGE_POINT_INACTIVE`（不泄露别人信息，且是教师
> 自己能改回来的输入问题，压成 404 反而无从下手）。`QuestionApiTest` 补了三个用例，
> 分别覆盖主知识点、次知识点两条分支和停用分支。
>
> **确认无问题的边界**：班级（`findAllOwned`/`findOwned`/`updateOwned`/`softDeleteOwned`/
> `hasActiveAssignmentsOwned` 全部含 `teacher_id`）、学生（读写均带 `teacher_id`，
> 或经 `exists (… c.teacher_id = …)`）、题目（`list`/`requireOwned` 含 `teacher_id`）、
> 作业（`list`/`requireOwned`）、评分结果（复核 SQL 连 `assignment a` 并过滤
> `a.teacher_id`）、练习单（`SELECT_SET` 恒带 `es.teacher_id`，`markApproved` 亦带）、
> 画像与错因排行（先经 `classrooms.get`／`assignments.requireOwned`）。
>
> **依赖前置校验而非内联 `teacher_id` 的写语句**（已确认都在同一次调用内先做过拥有关系校验，
> 属"可信拥有关系"）：`SubmissionImportService.persist` 的
> `update assignment … where id = :id`、`AiGradingTaskWorker` 的
> `update ai_grading_task … where id = :id`、`ExerciseRepository.items`/`rubrics` 按外键读子表。
> 这些语句本身不含 `teacher_id`，改动它们时要留意前置校验还在不在。
>
> **另发现一处非边界问题**：`classroom/OwnershipGuard.java` 是死代码——除了它自己的
> `OwnershipGuardTest` 之外无人调用，各服务的拥有关系校验都是各写各的。它名字像是
> "统一的教师边界机制"，留着会让后来的人误以为边界由它集中兜底。本轮未删（不在本步骤
> 范围内），列为待办。

- [x] **步骤 3：缩小前端生产分包**

Element Plus 改为自动按需导入或显式导入使用组件；ECharts 只注册柱状图、雷达图、标题、提示框、网格和 Canvas 渲染器。保持页面懒加载。

验收目标：不通过调高警告阈值掩盖问题；每个压缩前 JavaScript 分包低于 500 kB，或对仍超限的 MathJax 分包给出可复现说明。

> **实施记录（2026-09-15）**：改动前 `npm run build` 有两个分包超限并触发警告
> （主包 946.50 kB、AnalyticsView 1121.35 kB），`chunkSizeWarningLimit` 未被调高过。
>
> **Element Plus**：引入 `unplugin-vue-components` + `ElementPlusResolver` 做按需导入
> （`frontend/vite.config.ts`），`main.ts` 去掉 `app.use(ElementPlus)` 与整包 `dist/index.css`。
> 模板里实际只用到 7 个组件（dialog、form、form-item、input、input-number、option、select）。
> `ElMessage` 仍由各页面**显式 import**——测试要 `vi.mock('element-plus')` 拦它，
> 走自动导入会绕开 mock，所以只把它的样式单独引一次。
>
> **ECharts**：`AnalyticsView.vue` 从 `import * as echarts from 'echarts'` 改为
> `echarts/core` + 显式 `echarts.use([...])`。**未按计划注册雷达图、标题、提示框**：
> 全项目只有这一个图表，`setOption` 里没有 `radar`/`title`/`tooltip` 三项配置，
> 注册它们只会撑大分包（实测多 27 kB）。若后续真要加雷达图，届时在 `echarts.use` 里补
> `RadarChart` 即可。
>
> **测试环境**：按需导入插件会注入 element-plus 的样式模块，而样式模块 import 了
> `theme-chalk/*.css`；node_modules 外部化后由 Node 原生加载会报 `Unknown file extension
> ".css"`。`vite.config.ts` 的 `test.alias` 把这些 .css 指向空模块
> `src/testing/style-stub.ts`，并只内联 element-plus 的 `.../style/` 子路径。
> 起初写的是整体内联 `['element-plus']`，也能过，但 vitest 要多转换上千个模块，
> 测试从 6 秒涨到 23 秒；改成只内联样式模块后是 4.4 秒。`ExerciseView.test.ts` 的
> `vi.mock('element-plus')` 相应改为用 `importOriginal` 保留其余导出
> （模板组件被插件注入，整包 mock 掉会变成 undefined 直接渲染失败）。
>
> **结果**（`npm run build`，未调警告阈值，警告消失）：
>
> | 分包 | 改前 | 改后 |
> |---|---|---|
> | 主包 index | 946.50 kB | 163.44 kB |
> | AnalyticsView | 1121.35 kB | 466.85 kB |
> | CSS 合计 | 379.25 kB | 75.19 kB |
>
> **MathJax 说明**：`mathjax@4.1.3` 在 `package.json` 里，但全项目没有任何地方 import 它，
> 因此它不进入任何分包，计划里"对仍超限的 MathJax 分包给出说明"这一条不适用。
> 附带发现：题目内容里带 LaTeX（如 `$2x+1=5$`）但目前没有任何公式渲染，
> 页面上显示的是原始 `$...$` 文本。不在本步骤范围内，列为待办。
>
> 页面懒加载保持不变：每个 view 仍是独立分包，路由未改。
>
> **按需导入引入的冷启动回归（已修）**：按需导入上线后，E2E 连跑三次、三次结果不同。
> 从 dev server 日志定位到根因：依赖扫描只认入口的静态 import，而按需导入插件是在
> transform 阶段才把 `element-plus/es` 和 `.../style/css` 注入模板的，`.vue` 内部的 import
> 本身也不在扫描范围内（`AnalyticsView.vue` 里的 echarts 同样漏掉）。于是冷启动后 Vite 在
> 运行中才发现这些依赖 → 重新预打包 → 给已连接的客户端发整页刷新，日志为
> `optimized dependencies changed. reloading`；那一次刷新打断正在进行的路由跳转，
> 表现为 `Failed to fetch dynamically imported module: .../ReviewView.vue`，
> 页面停在原地。`playwright.config.ts` 没有配 `retries`，所以这个竞态直接表现为随机失败。
>
> 第一次修错了：写了 `optimizeDeps: { include: [/^element-plus\/es(\/.*)?$/] }`。
> Vite 的 `DepOptimizationConfig.include` 只接受**可解析的 import 路径、不支持 glob**
> （d.ts 原文 "cannot be globs"），传正则不会报配置错误，而是启动时
> `TypeError: id.replace is not a function` 直接把 dev server 打挂。
>
> 正确修法是 `server.warmup.clientFiles: ['./src/**/*.vue']`：启动阶段先把 .vue 全量转换一遍，
> 让依赖发现在浏览器连上来之前自然发生。之所以不去 `include` 里逐个列组件样式路径：
> 那样等于把"模板里用了哪些组件"再维护一份，加一个组件就复发；预热对新增组件、新图表库
> 都自动生效。
>
> 验证：清空 `node_modules/.vite` 后冷启动两次，`.vite/deps` 下 27 个产物已包含全部
> `element-plus_es_*_style_css` 与 `echarts_*`，dev server 日志中 `reloading` /
> `Failed to fetch` 出现 0 次，两次 `npm run test:e2e` 均首次通过（4.0s / 4.1s）。

- [x] **步骤 4：重新运行完整自动化矩阵**

```powershell
mvn -f backend/pom.xml -q test
npm --prefix frontend run test
npm --prefix frontend run typecheck
npm --prefix frontend run build
python -m unittest discover -s evaluation/tests -v
```

> **实施记录（2026-09-15）**：全部通过。
>
> | 命令 | 结果 |
> |---|---|
> | `mvn -f backend/pom.xml -q test` | 22 个测试类 / 71 个用例，0 失败 0 错误 0 跳过（`-q` 不打印汇总行，数字从 `backend/target/surefire-reports/*.txt` 汇总） |
> | `npm --prefix frontend run test` | 4 个文件 / 20 个用例（新增 `errorTypes.test.ts` 3 个） |
> | `npm --prefix frontend run typecheck` | `vue-tsc -b` 无输出，通过 |
> | `npm --prefix frontend run build` | 通过，无分包超限警告；index 163.47 kB、AnalyticsView 466.75 kB |
> | `python -m unittest discover -s evaluation/tests -v` | 17 个用例 OK（0.05s） |
> | `mvn -f backend/pom.xml verify -Pmysql-it` | 7 个用例（MySqlCoreWorkflowIT 4 + MySqlMigrationIT 3），0 失败，BUILD SUCCESS，1 分 36 秒 |
>
> 记一个容易误读的数字：多个套件并发跑时 vitest 会从 ~7 秒涨到 26.5 秒（CPU 争用，
> `environment` 占 92%），单跑是 6.79～7.46 秒。这个耗时不是回归。

Docker 可用且已确认使用一次性容器时再运行：

```powershell
mvn -f backend/pom.xml verify -Pmysql-it
```

- [x] **步骤 5：运行真实浏览器验收**

使用 `demo` 配置启动后端和前端，执行：

```powershell
npm --prefix frontend run test:e2e
```

人工检查 1440×900 和 390×844 两种视口下的登录、空状态、错误状态、表格滚动、复核表单、画像图表和练习导出。

> **实施记录（2026-09-15）**：`demo` 配置（H2 内存库）后端 8080 + vite 5173，
> `npm run test:e2e` 多次运行均首次通过（3.8s / 4.0s / 4.1s / 4.7s），无重试。
>
> **两种视口的逐页检查**：环境里没有可交互的浏览器，改为用 Playwright 驱动 Chrome 逐页
> 截图后查看，并顺带量化了几个指标（横向溢出、越界元素、移动端导航可达性），
> 共 27 张：登录、仪表盘、作业管理、数学题库、教师复核、班级与学生、学情分析、
> 分层练习（未生成）、分层练习（已生成三层）、错误状态，外加移动端每次展开的抽屉。
>
> - 横向溢出 **0 / 27**：`documentElement.scrollWidth` 与视口宽度逐一相等。
> - 移动端（390×844）导航是抽屉式的，点"菜单"后各导航项才进入视口；逐个点开验证过可达。
>   （首次脚本直接点导航项失败并报 `element is outside of the viewport`，是脚本没先展开抽屉，
>   不是缺陷；展开按钮的 `aria-label="打开导航"` 覆盖了文本"菜单"，只能按 class 定位。）
> - 移动端表格改为卡片堆叠，无裁切；图表随容器重绘，两种视口下都正常。
> - 错误状态：把 `/api` 全部掐断后，页面给出"服务暂时不可用，请检查后端是否已启动。"
>   + 空状态面板，没有白屏（`desktop-10-error-state`）。
> - 空状态：分层练习显示"还没有练习单 / 选好来源班级与作业后点击"生成分层练习"。"。
>   注意 `demo` 用内存库，反复点"生成分层练习"会在同一次后端生命周期内累积草稿，
>   要看真正的空状态得重启后端；本轮为此重启过一次。
>
> **本轮查出并修掉两个缺陷**（都属于"英文枚举/文案直接暴露给教师"这一类）：
>
> 1. **错因标签漏了 `ANSWER_MISMATCH`**。`AnalyticsView.vue` 里的 `labelMap` 只有 5 项，
>    而"高频问题"面板把 `ANSWER_MISMATCH` 原样显示成英文——偏偏客观题答错走的就是
>    `ObjectiveRuleGrader` 的这条分支，是出现频率最高的错因。根因是标签表只覆盖了
>    `AiGradingTaskWorker.ERROR_TYPES` 那套（CORRECT + 5 个），漏了规则判定这套。
>    修法：标签表收敛到 `frontend/src/api/errorTypes.ts` 一处（7 个取值），
>    `AnalyticsView` 与 `ReviewView` 的"建议错因"都改用它，并加
>    `src/api/errorTypes.test.ts` 把 7 个取值钉住，后端加取值时这个测试会先失败。
>    验收截图里已从 `ANSWER_MISMATCH` 变成"答案不一致 3 次"。
> 2. **题库检索无结果时文案误导**。`QuestionView.vue` 只看 `shown.length === 0`，
>    于是搜一个不存在的题号会显示"题库暂无内容 / 录入第一道题"，
>    教师会以为整个题库被清空了。修法：区分"检索无结果"（"没有匹配的题目，换个题号或
>    题干关键词再试"）与"题库确实为空"，两种视口都已复核。
>
> **本轮未做、留待办**：`/favicon.ico` 返回 404（无 `public/` 目录、`index.html` 也没声明
> favicon，浏览器自动请求所致，控制台里那条 404 就是它）；题目里的 LaTeX 仍按原文显示；
> 移动端"生成分层练习"按钮文案会折成三行；复核表单的"最终错因"是自由文本输入框，
> 里面仍是英文枚举，改成下拉属于接口契约变更，不在本轮范围。
>
> **未验证项（不夸大）**：只跑了 `demo` 内存库，没有对真实 MySQL 做持久化验证；
> 真实模型未调用（demo 里的 AI 判定结果来自种子数据，`source='AI'` 是构造出来的）。

- [x] **步骤 6：进行最终仓库审查**

```powershell
git diff --check
git status --short
rg -n "MODEL_API_KEY|Bearer [A-Za-z0-9_-]{16,}|password=" . -g '!frontend/node_modules/**' -g '!backend/target/**'
```

确认没有真实凭据、测试下载文件、生成目录噪音和无关改动。

> **实施记录（2026-09-15）**：`git diff --check` 无空白错误（只有本仓库在 Windows 上的
> LF→CRLF 常规提示，退出码 0）。凭据扫描命中的 5 处全部是占位或说明：
> `.env.example` 的 `replace-with-provider-key`、`application.yml` 的 `${MODEL_API_KEY:}`、
> `OpenAiCompatibleModelClient` 的报错文案、`README.md` 的说明、以及计划里这条命令本身。
> 没有真实密钥。
>
> 未跟踪文件逐个确认都是本轮该交付的内容，没有噪音：
> `evaluation/`（脚本、README、两个示例 CSV、测试）、`docs/experiments/实验记录模板.md`、
> `GeneratedKeysConcurrencyTest.java`、`frontend/src/api/errorTypes.{ts,test.ts}`、
> `frontend/src/components.d.ts`（按需导入插件生成的类型声明，必须提交，否则先跑
> typecheck 会报错）、`frontend/src/testing/style-stub.ts`。
> `evaluation/output/` 与 `__pycache__/` 已加进 `.gitignore`，`frontend/dist/`、
> `node_modules/.vite` 本来就在忽略范围内。验收用的截图与一次性脚本都写在系统临时目录
> （`%TEMP%\vischeck\`），没有落进仓库；E2E 下载的 Word 由 Playwright 自己管在临时目录。

- [x] **步骤 7：更新中文交付文档**

阶段总结只把本轮确实运行通过的项目标记为已验证；真实模型未调用时明确写“适配器契约已验证，真实服务未验证”；未执行持久化 MySQL 时不得写成已经部署。

> **实施记录（2026-09-15）**：
>
> - `README.md`：已实现功能补上第二阶段交付的分层练习与 Word 导出、离线评测脚本；
>   技术栈注明 Element Plus 按需导入与 ECharts 只注册用到的图表；验证命令补上
>   `python -m unittest discover -s evaluation/tests -v` 并说明评测输入冻结、结果可复现；
>   目录说明补上 `evaluation/` 与 `docs/experiments/`。
> - `docs/2026-09-14-项目阶段总结.md`：新增第八节"第二阶段完成情况与验证快照"，
>   逐项列出本轮**确实跑过**的结果；表中两行刻意写成未完成——
>   "持久化 MySQL 部署：**未执行**，只验证了一次性容器"、
>   "真实大模型：**未调用**，适配器契约已验证，真实服务未验证"。
>   同时改掉两处会误导读者的地方：第四节标题下注明那是第一阶段快照、
>   结论已被第二阶段修复；第三节表格的"Flyway 管理 5 个迁移版本"改为 6 个（V1–V6）。
>   第二阶段仍存在的问题（LaTeX 原文显示、favicon 404、最终错因自由文本、
>   `OwnershipGuard` 死代码、H2 版本高于 Flyway 已验证版本）一并列出。

---

## 里程碑与停止条件

### 里程碑一：可信回归基线

完成任务一、二。后端全量测试、前端单元测试、类型检查和构建必须稳定通过。未达到此条件，不开始分层练习开发。

### 里程碑二：外部边界可验证

完成任务三、四。模型适配器使用本地模拟服务验证；MySQL 使用一次性容器验证。二者都不需要真实业务数据。

### 里程碑三：教学闭环增强

完成任务五、六。教师能基于已确认画像生成、确认并下载三级练习 Word 文档。

### 里程碑四：论文与答辩就绪

完成任务七、八。实验脚本、匿名样本格式、自动化验证和中文交付文档齐全。

## 明确不在本计划中的内容

- 拍照识别、手写公式 OCR；
- RAG、向量数据库、知识库问答；
- 学生端、家长端、消息推送；
- 自动替教师发布成绩或把 AI 建议直接写成正式成绩；
- 多学科扩展、微服务拆分、云端部署；
- 未经授权的真实模型调用、持久化数据库迁移、Git 提交或推送。

## 计划自审结果

- 现有四个主要缺口分别由任务一至七覆盖，任务八负责收口；
- 文件路径、接口名、状态值和验证命令在各任务间保持一致；
- 计划没有把真实 MySQL、真实模型和分层练习描述成当前已完成能力；
- 每个任务都能独立形成可测试产物，且存在明确的开始条件和验收标准。
