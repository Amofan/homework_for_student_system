# 初中数学作业分析系统第三阶段（论文实验与交付收口）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不扩展学生端、OCR、RAG 等范围的前提下，收紧错因数据契约，补齐可审计的论文样本导出与教师端下载入口，完成正式匿名样本实验和答辩级验收。

**Architecture:** 保持现有 Spring JDBC + Vue 单体结构，不新增服务或数据库表。后端用一个共享错因枚举约束规则评分、AI 提示词和教师复核；评测 CSV 在兼容既有列的基础上追加复核决策与模型溯源字段；前端仍通过鉴权后的 Axios 请求下载 Blob，并从响应头读取导出完整性计数。真实实验数据只保存在仓库外受控目录，仓库内只提交协议、脚本、示例数据和不含真实样本的验证记录。

**Tech Stack:** Java 21、Spring Boot 4.1.1、Spring Security、Spring JDBC、Flyway、MySQL 8.4、H2 2.3.232、Vue 3、TypeScript、Vite、Element Plus、Vitest、Playwright、Python 3 标准库。

**Spec:** `docs/superpowers/specs/2026-09-14-junior-math-homework-analysis-design.md`

## Global Constraints

- 大模型输出始终只是建议；只有教师确认后的结果进入画像、分层练习和论文真值。
- 错因标签固定为 `CORRECT`、`ANSWER_MISMATCH`、`CALCULATION_ERROR`、`METHOD_ERROR`、`CONCEPT_ERROR`、`INCOMPLETE`、`OTHER`；AI 只允许除 `ANSWER_MISMATCH` 外的六项。
- 模型请求和论文导出不得包含姓名、学号、班级名、学校名、JWT、模型密钥或身份映射表。
- 不新增学生端、家长端、管理员端、在线考试、OCR、RAG、向量数据库、Markdown 或 OMML。
- 不对已有 Flyway 迁移做原地修改；本阶段预计不需要新迁移。
- 不把真实匿名样本、真实结果 JSON 或身份映射表提交进 Git；统一保存到 `E:\homework-analysis-experiment-archive\2026-09-main-study`。
- 执行持久化 MySQL 迁移、真实模型批量调用或真实样本导出前，必须再次说明影响、费用、备份和回滚方式并取得用户明确确认。
- 每个任务先写会失败的测试，再做最小实现；只暂存本任务文件，不夹带其他工作树改动。

## File Structure

### 新建

- `backend/src/main/java/com/homework/analysis/grading/ErrorType.java`：唯一的后端错因枚举与 AI 允许集合。
- `backend/src/test/java/com/homework/analysis/grading/ErrorTypeTest.java`：固定七项全集和六项 AI 子集。
- `frontend/src/views/ReviewView.test.ts`：教师复核的错因下拉与提交契约。
- `frontend/src/views/AssignmentView.test.ts`：评测样本下载与完整性提示。
- `backend/src/main/java/com/homework/analysis/evaluation/EvaluationExport.java`：CSV 内容及导出计数。
- `frontend/public/favicon.svg`：消除 `/favicon.ico` 请求并保留产品识别。
- `docs/experiments/2026-09-15-论文正式实验方案.md`：冻结研究问题、样本规则、排除规则和归档方法。

### 修改

- `backend/src/main/java/com/homework/analysis/grading/ObjectiveRuleGrader.java`：返回共享错因枚举值。
- `backend/src/main/java/com/homework/analysis/grading/RuleGrade.java`：把错因字段从任意字符串收紧为 `ErrorType`。
- `backend/src/main/java/com/homework/analysis/grading/GradingOrchestrator.java`：把枚举以稳定字符串写入数据库。
- `backend/src/main/java/com/homework/analysis/grading/ai/AiGradingTaskWorker.java`：使用共享 AI 错因子集。
- `backend/src/main/java/com/homework/analysis/grading/review/TeacherReviewService.java`：拒绝未知最终错因。
- `backend/src/main/java/com/homework/analysis/evaluation/GradingCaseRow.java`：加入复核决策、模型和提示词版本。
- `backend/src/main/java/com/homework/analysis/evaluation/GradingCaseCsv.java`：追加 CSV v2 列。
- `backend/src/main/java/com/homework/analysis/evaluation/EvaluationExportService.java`：返回 CSV 与完整性计数。
- `backend/src/main/java/com/homework/analysis/evaluation/EvaluationExportController.java`：输出完整性响应头并暴露给浏览器。
- `backend/src/test/java/com/homework/analysis/grading/review/TeacherReviewApiTest.java`：覆盖未知错因拒绝。
- `backend/src/test/java/com/homework/analysis/evaluation/EvaluationExportApiTest.java`：覆盖 v2 列、计数头和溯源字段。
- `frontend/src/api/errorTypes.ts`：导出强类型错因选项。
- `frontend/src/api/types.ts`：收紧 `ReviewQueueItem.errorType` 类型。
- `frontend/src/api/client.ts`：复用安全 Blob 下载并新增评测下载函数。
- `frontend/src/views/ReviewView.vue`：自由文本改成固定下拉框。
- `frontend/src/views/AssignmentView.vue`：增加“导出评测样本”入口和结果提示。
- `frontend/index.html`：声明 SVG favicon。
- `frontend/package.json`：不变；本阶段不增加前端依赖。
- `backend/pom.xml`：将 H2 固定到 Flyway 已验证的 `2.3.232`。
- `evaluation/evaluate_grading.py`：解析 v2 字段并输出三种复核决策比例及模型溯源摘要。
- `evaluation/tests/test_evaluate_grading.py`：覆盖新口径和字段一致性校验。
- `evaluation/data/grading_cases.example.csv`：升级为 v2 示例。
- `evaluation/README.md`：记录 CSV v2、响应头、归档与正式实验命令。
- `docs/experiments/2026-09-14-实验记录模板.md`：增加复核决策、模型/提示词版本和剔除计数。
- `README.md`：更新教师端导出入口、验证基线和正式实验边界。
- `docs/2026-09-14-项目阶段总结.md`：追加第三阶段完成情况，保留前两阶段历史快照。

### 删除

- `backend/src/main/java/com/homework/analysis/classroom/OwnershipGuard.java`：当前无生产调用方。
- `backend/src/test/java/com/homework/analysis/classroom/OwnershipGuardTest.java`：仅测试已删除死代码。

---

### Task 1: 收口后端错因标签契约

**Files:**
- Create: `backend/src/main/java/com/homework/analysis/grading/ErrorType.java`
- Create: `backend/src/test/java/com/homework/analysis/grading/ErrorTypeTest.java`
- Modify: `backend/src/main/java/com/homework/analysis/grading/ObjectiveRuleGrader.java`
- Modify: `backend/src/main/java/com/homework/analysis/grading/RuleGrade.java`
- Modify: `backend/src/main/java/com/homework/analysis/grading/GradingOrchestrator.java`
- Modify: `backend/src/main/java/com/homework/analysis/grading/ai/AiGradingTaskWorker.java`
- Modify: `backend/src/main/java/com/homework/analysis/grading/review/TeacherReviewService.java`
- Test: `backend/src/test/java/com/homework/analysis/grading/review/TeacherReviewApiTest.java`

**Interfaces:**
- Produces: `ErrorType` 七项枚举。
- Produces: `ErrorType.aiCodes(): List<String>`，固定返回模型可用的六个字符串编码。
- Produces: `ErrorType.isKnown(String): boolean`，供教师复核边界校验。
- Consumes: 数据库仍保存 `varchar(64)`；不创建迁移。

- [ ] **Step 1: 写共享枚举的失败测试**

```java
class ErrorTypeTest {
    @Test
    void 固定完整标签全集() {
        assertThat(Arrays.stream(ErrorType.values()).map(Enum::name))
            .containsExactlyInAnyOrder("CORRECT", "ANSWER_MISMATCH", "CALCULATION_ERROR",
                "METHOD_ERROR", "CONCEPT_ERROR", "INCOMPLETE", "OTHER");
    }

    @Test
    void 模型不允许使用客观题专属标签() {
        assertThat(ErrorType.aiCodes())
            .containsExactly("CORRECT", "CALCULATION_ERROR", "METHOD_ERROR",
                "CONCEPT_ERROR", "INCOMPLETE", "OTHER")
            .doesNotContain("ANSWER_MISMATCH");
    }
}
```

- [ ] **Step 2: 运行测试并确认因为 `ErrorType` 不存在而失败**

Run: `mvn -f backend/pom.xml -Dtest=ErrorTypeTest test`

Expected: 编译失败，提示找不到 `ErrorType`。

- [ ] **Step 3: 创建最小共享枚举**

```java
package com.homework.analysis.grading;

import java.util.Arrays;
import java.util.List;

public enum ErrorType {
    CORRECT(true),
    ANSWER_MISMATCH(false),
    CALCULATION_ERROR(true),
    METHOD_ERROR(true),
    CONCEPT_ERROR(true),
    INCOMPLETE(true),
    OTHER(true);

    private final boolean aiAllowed;

    ErrorType(boolean aiAllowed) {
        this.aiAllowed = aiAllowed;
    }

    public static List<String> aiCodes() {
        return Arrays.stream(values()).filter(value -> value.aiAllowed)
            .map(Enum::name).toList();
    }

    public static boolean isKnown(String code) {
        if (code == null) return false;
        return Arrays.stream(values()).anyMatch(value -> value.name().equals(code));
    }
}
```

- [ ] **Step 4: 让规则评分和 AI 任务使用共享枚举**

`ObjectiveRuleGrader.grade(...)` 返回：

```java
return new RuleGrade(correct ? totalScore : 0,
    correct ? ErrorType.CORRECT : ErrorType.ANSWER_MISMATCH);
```

把 `RuleGrade` 的第二个字段改为 `ErrorType errorType`，`GradingOrchestrator` 写库时使用 `result.errorType().name()`；教师说明和反馈也按枚举比较。删除 `AiGradingTaskWorker.ERROR_TYPES`，构造请求时使用 `ErrorType.aiCodes()`。

- [ ] **Step 5: 写未知最终错因的失败接口测试**

```java
@Test
void 修改复核不能写入标签全集以外的错因() throws Exception {
    mvc.perform(post("/api/grading/results/" + RESULT_ID + "/review")
            .header("Authorization", bearer())
            .contentType(MediaType.APPLICATION_JSON)
            .content("""
                {"decision":"MODIFY","finalScore":6,"errorType":"手滑写错",\
                 "reason":"修正模型判断"}
                """))
        .andExpect(status().isBadRequest())
        .andExpect(jsonPath("$.error.code").value("REVIEW_ERROR_TYPE_INVALID"));

    assertThat(jdbc.queryForObject(
        "select status from grading_result where id = 701", String.class))
        .isEqualTo("PENDING_REVIEW");
}
```

- [ ] **Step 6: 在事务写入前校验非采纳决策的最终错因**

在 `TeacherReviewService.review(...)` 完成回退取值后、范围校验前增加：

```java
if (!ErrorType.isKnown(errorType)) {
    throw new DomainException("REVIEW_ERROR_TYPE_INVALID", "最终错因必须从固定标签中选择");
}
```

`ACCEPT` 仍沿用原建议错因；测试种子中的原始错因必须全部属于固定集合。

- [ ] **Step 7: 运行针对性测试和后端全量测试**

Run:

```powershell
mvn -f backend/pom.xml -Dtest=ErrorTypeTest,ObjectiveRuleGraderTest,TeacherReviewApiTest,AiGradingTaskWorkerTest test
mvn -f backend/pom.xml test
```

Expected: 针对性测试与全量测试均通过；未知错因返回 400，数据库状态不变。

- [ ] **Step 8: 提交后端错因契约**

```powershell
git add backend/src/main/java/com/homework/analysis/grading/ErrorType.java `
  backend/src/main/java/com/homework/analysis/grading/ObjectiveRuleGrader.java `
  backend/src/main/java/com/homework/analysis/grading/RuleGrade.java `
  backend/src/main/java/com/homework/analysis/grading/GradingOrchestrator.java `
  backend/src/main/java/com/homework/analysis/grading/ai/AiGradingTaskWorker.java `
  backend/src/main/java/com/homework/analysis/grading/review/TeacherReviewService.java `
  backend/src/test/java/com/homework/analysis/grading/ErrorTypeTest.java `
  backend/src/test/java/com/homework/analysis/grading/review/TeacherReviewApiTest.java
git commit -m "fix: enforce grading error type contract"
```

### Task 2: 把教师复核的自由文本错因改成固定选项

**Files:**
- Modify: `frontend/src/api/errorTypes.ts`
- Modify: `frontend/src/api/types.ts`
- Modify: `frontend/src/views/ReviewView.vue`
- Create: `frontend/src/views/ReviewView.test.ts`
- Test: `frontend/src/api/errorTypes.test.ts`

**Interfaces:**
- Produces: `ErrorType` TypeScript 联合类型。
- Produces: `ERROR_TYPE_OPTIONS: ReadonlyArray<{ value: ErrorType; label: string }>`。
- Consumes: Task 1 的七项后端集合；编码必须逐项一致。

- [ ] **Step 1: 扩展前端错因模块测试**

```ts
it('复核下拉包含且只包含七个后端取值', () => {
  expect(ERROR_TYPE_OPTIONS.map(option => option.value)).toEqual([
    'CORRECT', 'ANSWER_MISMATCH', 'CALCULATION_ERROR', 'METHOD_ERROR',
    'CONCEPT_ERROR', 'INCOMPLETE', 'OTHER',
  ])
})
```

- [ ] **Step 2: 运行并确认导出不存在**

Run: `npm --prefix frontend run test -- src/api/errorTypes.test.ts`

Expected: FAIL，提示 `ERROR_TYPE_OPTIONS` 未导出。

- [ ] **Step 3: 导出强类型选项并收紧 API 类型**

```ts
export const ERROR_TYPE_OPTIONS = [
  { value: 'CORRECT', label: '正确' },
  { value: 'ANSWER_MISMATCH', label: '答案不一致' },
  { value: 'CALCULATION_ERROR', label: '计算错误' },
  { value: 'METHOD_ERROR', label: '方法错误' },
  { value: 'CONCEPT_ERROR', label: '概念错误' },
  { value: 'INCOMPLETE', label: '过程不完整' },
  { value: 'OTHER', label: '其他' },
] as const

export type ErrorType = typeof ERROR_TYPE_OPTIONS[number]['value']
```

从选项派生标签映射和 `KNOWN_ERROR_TYPES`，不要再手写第二份对象。把 `ReviewQueueItem.errorType` 改为 `ErrorType`；`errorTypeLabel` 仍接收 `string`，以便未知服务端值能原样显示而不是静默映射成“其他”。

- [ ] **Step 4: 写复核页面失败测试**

测试挂载一个 `errorType: 'METHOD_ERROR'` 的待复核项，断言：

```ts
expect(wrapper.findComponent({ name: 'ElSelect' }).exists()).toBe(true)
expect(wrapper.text()).toContain('方法错误')
expect(wrapper.find('input[aria-label="最终错因自由文本"]').exists()).toBe(false)
```

同时触发“修改”提交，断言请求体中的 `errorType` 是固定编码 `CONCEPT_ERROR`，不是中文标签。

- [ ] **Step 5: 用 `el-select` 替换自由文本框**

```vue
<label>最终错因
  <el-select v-model="form.errorType" placeholder="选择最终错因">
    <el-option
      v-for="option in ERROR_TYPE_OPTIONS"
      :key="option.value"
      :label="option.label"
      :value="option.value"
    />
  </el-select>
</label>
```

`form.errorType` 类型改为 `ErrorType`；`select(item)` 继续以建议值初始化。提交按钮在 `decision !== 'ACCEPT'` 且最终错因为空时禁用，避免无效请求。

- [ ] **Step 6: 运行前端测试、类型检查与构建**

Run:

```powershell
npm --prefix frontend run test -- src/api/errorTypes.test.ts src/views/ReviewView.test.ts
npm --prefix frontend run test
npm --prefix frontend run typecheck
npm --prefix frontend run build
```

Expected: 测试、类型检查和生产构建全部通过；构建不出现 500 kB 分块警告。

- [ ] **Step 7: 提交教师复核选项**

```powershell
git add frontend/src/api/errorTypes.ts frontend/src/api/errorTypes.test.ts `
  frontend/src/api/types.ts frontend/src/views/ReviewView.vue `
  frontend/src/views/ReviewView.test.ts frontend/src/components.d.ts
git commit -m "fix: constrain teacher review error types"
```

### Task 3: 升级论文评测 CSV 与指标口径

**Files:**
- Modify: `backend/src/main/java/com/homework/analysis/evaluation/GradingCaseRow.java`
- Modify: `backend/src/main/java/com/homework/analysis/evaluation/GradingCaseCsv.java`
- Modify: `backend/src/main/java/com/homework/analysis/evaluation/EvaluationExportService.java`
- Test: `backend/src/test/java/com/homework/analysis/evaluation/EvaluationExportApiTest.java`
- Modify: `evaluation/evaluate_grading.py`
- Modify: `evaluation/tests/test_evaluate_grading.py`
- Modify: `evaluation/data/grading_cases.example.csv`
- Modify: `evaluation/README.md`

**Interfaces:**
- Produces CSV columns: existing 11 columns followed by `review_decision,model_name,prompt_version`.
- Produces JSON `review_decision_rate` with `accept`、`modify`、`reject`.
- Produces JSON `provenance` with sorted `model_names`、`prompt_versions` and missing counts.
- Preserves: `teacher_modified` remains in CSV and must equal `review_decision != ACCEPT`.

- [ ] **Step 1: 先修改后端导出测试的期望契约**

```java
assertThat(lines[0]).isEqualTo("case_id,total_score,teacher_score,ai_score,teacher_error_type,"
    + "ai_error_type,teacher_modified,teacher_seconds,ai_seconds,input_tokens,output_tokens,"
    + "review_decision,model_name,prompt_version");
assertThat(lines[1]).isEqualTo("answer-611,10,6,8,CALCULATION_ERROR,METHOD_ERROR,true,"
    + "42.5,3.799,513,604,MODIFY,qwen,v1");
```

把种子任务补上 `prompt_version='v1'`。另加一行 `REJECT` 样本，证明拒绝不会被折叠成普通修改。

- [ ] **Step 2: 运行后端导出测试并确认表头失败**

Run: `mvn -f backend/pom.xml -Dtest=EvaluationExportApiTest test`

Expected: FAIL，实际 CSV 缺少三个新列。

- [ ] **Step 3: 从已有表连接中导出复核与模型溯源字段**

把 `GradingCaseRow` 末尾增加：

```java
String reviewDecision,
String modelName,
String promptVersion
```

`EvaluationExportService.reviewedCases(...)` 查询加入：

```sql
tr.decision, t.model_name, t.prompt_version
```

`GradingCaseCsv` 先保留原 11 列顺序，再依次追加三个字段；所有字符串继续通过现有 `quote(...)` 转义。

- [ ] **Step 4: 运行后端导出测试**

Run: `mvn -f backend/pom.xml -Dtest=EvaluationExportApiTest test`

Expected: PASS；CSV 不含学生身份信息，空的模型字段仍输出为空单元格。

- [ ] **Step 5: 写 Python v2 读取与指标失败测试**

```python
def test_review_decision_rates_keep_accept_modify_and_reject_separate(self):
    cases = [
        eg.GradingCase("C-1", 10, 8, 8, "CORRECT", "CORRECT", False,
                       40, 8, 500, 100, "ACCEPT", "model-a", "v1"),
        eg.GradingCase("C-2", 10, 6, 8, "METHOD_ERROR", "CALCULATION_ERROR", True,
                       60, 9, 520, 110, "MODIFY", "model-a", "v1"),
        eg.GradingCase("C-3", 10, 5, 8, "CONCEPT_ERROR", "METHOD_ERROR", True,
                       70, 10, 530, 120, "REJECT", "model-a", "v1"),
        eg.GradingCase("C-4", 10, 10, 10, "CORRECT", "CORRECT", False,
                       30, 7, 490, 90, "ACCEPT", "model-a", "v1"),
    ]
    self.assertEqual(eg.evaluate(cases)["review_decision_rate"], {
        "accept": 0.5, "modify": 0.25, "reject": 0.25,
    })

def test_teacher_modified_must_match_review_decision(self):
    with tempfile.TemporaryDirectory() as raw:
        path = Path(raw) / "inconsistent.csv"
        path.write_text(
            "case_id,total_score,teacher_score,ai_score,teacher_error_type,"
            "ai_error_type,teacher_modified,teacher_seconds,ai_seconds,input_tokens,"
            "output_tokens,review_decision,model_name,prompt_version\n"
            "C-1,10,6,8,METHOD_ERROR,CALCULATION_ERROR,false,60,9,520,110,"
            "MODIFY,deepseek-v4-pro,v1\n",
            encoding="utf-8",
        )
        with self.assertRaisesRegex(
            ValueError, "teacher_modified 与 review_decision 不一致"
        ):
            eg.read_grading_cases(path)
```

再覆盖多个模型/提示词版本排序、模型名缺失计数和非法复核决策。

- [ ] **Step 6: 扩展 Python 数据类、解析与输出**

`GradingCase` 增加：

```python
review_decision: str
model_name: str | None
prompt_version: str | None
```

读取时只接受 `ACCEPT`、`MODIFY`、`REJECT`，并执行：

```python
if teacher_modified != (review_decision != "ACCEPT"):
    raise ValueError(f"{path} 的 teacher_modified 与 review_decision 不一致")
```

`evaluate(...)` 增加：

```python
"review_decision_rate": {
    "accept": _round(_rate(decisions.count("ACCEPT"), len(cases))),
    "modify": _round(_rate(decisions.count("MODIFY"), len(cases))),
    "reject": _round(_rate(decisions.count("REJECT"), len(cases))),
},
"provenance": {
    "model_names": sorted({case.model_name for case in cases if case.model_name}),
    "prompt_versions": sorted({case.prompt_version for case in cases if case.prompt_version}),
    "missing_model_name_count": sum(case.model_name is None for case in cases),
    "missing_prompt_version_count": sum(case.prompt_version is None for case in cases),
},
```

保留 `teacher_modification_rate`，其值必须等于 `modify + reject`，避免论文旧章节数字失去可比性。

- [ ] **Step 7: 升级示例 CSV 与文档**

示例数据八行都补齐 `review_decision`、`model_name=example-model`、`prompt_version=v1`；其中 `teacher_modified=false` 的行使用 `ACCEPT`，其余行在 `MODIFY` 与 `REJECT` 间明确分配。`evaluation/README.md` 解释旧字段保留原因和三个新字段的缺失语义。

- [ ] **Step 8: 运行评测测试和示例数据端到端命令**

Run:

```powershell
python -m unittest discover -s evaluation/tests -v
python evaluation/evaluate_grading.py `
  --grading evaluation/data/grading_cases.example.csv `
  --labels evaluation/data/error_labels.example.csv `
  --output evaluation/output/metrics-v2.json
```

Expected: 全部测试通过；`sample_count=8`；`teacher_modification_rate` 等于三个决策比例中 `modify + reject`；溯源只包含 `example-model` 和 `v1`。

- [ ] **Step 9: 运行后端全量测试并提交**

```powershell
mvn -f backend/pom.xml test
git add backend/src/main/java/com/homework/analysis/evaluation `
  backend/src/test/java/com/homework/analysis/evaluation/EvaluationExportApiTest.java `
  evaluation/evaluate_grading.py evaluation/tests/test_evaluate_grading.py `
  evaluation/data/grading_cases.example.csv evaluation/README.md
git commit -m "feat: preserve evaluation decisions and provenance"
```

### Task 4: 增加可审计的一键评测样本下载

**Files:**
- Create: `backend/src/main/java/com/homework/analysis/evaluation/EvaluationExport.java`
- Modify: `backend/src/main/java/com/homework/analysis/evaluation/EvaluationExportService.java`
- Modify: `backend/src/main/java/com/homework/analysis/evaluation/EvaluationExportController.java`
- Test: `backend/src/test/java/com/homework/analysis/evaluation/EvaluationExportApiTest.java`
- Modify: `frontend/src/api/client.ts`
- Modify: `frontend/src/views/AssignmentView.vue`
- Create: `frontend/src/views/AssignmentView.test.ts`

**Interfaces:**
- Produces: `EvaluationExport(String csv, int reviewedAiCount, int exportedCount, int skippedMissingAiErrorCount)`.
- Produces response headers: `X-Evaluation-Reviewed`、`X-Evaluation-Exported`、`X-Evaluation-Skipped-Missing-Ai-Error`.
- Produces: `dispositionFilename(disposition: unknown, fallback: string): string` 与 `saveBlob(blob: Blob, filename: string): void`，供两种下载复用。
- Produces: `downloadEvaluationCases(assignmentId): Promise<EvaluationDownloadSummary>`.
- Preserves: CSV 响应体仍可原样作为 `evaluate_grading.py --grading` 输入。

- [ ] **Step 1: 写后端完整性计数失败测试**

在已有“缺少模型原始错因的样本被剔除”场景中断言：

```java
.andExpect(header().string("X-Evaluation-Reviewed", "2"))
.andExpect(header().string("X-Evaluation-Exported", "1"))
.andExpect(header().string("X-Evaluation-Skipped-Missing-Ai-Error", "1"))
.andExpect(header().string(HttpHeaders.ACCESS_CONTROL_EXPOSE_HEADERS,
    "Content-Disposition, X-Evaluation-Reviewed, X-Evaluation-Exported, "
        + "X-Evaluation-Skipped-Missing-Ai-Error"));
```

- [ ] **Step 2: 运行测试并确认响应头缺失**

Run: `mvn -f backend/pom.xml -Dtest=EvaluationExportApiTest test`

Expected: FAIL，四个响应头均不存在。

- [ ] **Step 3: 返回 CSV 与计数并写入响应头**

```java
public record EvaluationExport(
    String csv,
    int reviewedAiCount,
    int exportedCount,
    int skippedMissingAiErrorCount) {
}
```

`EvaluationExportService` 用 `reviewed.size()`、`cases.size()`、`skipped` 构造结果。控制器仍返回 UTF-8 CSV 字节，并附加四个响应头。保留 WARN 日志，但浏览器不再依赖服务端日志才能知道剔除数量。

- [ ] **Step 4: 写前端下载失败测试**

`AssignmentView.test.ts` mock `downloadEvaluationCases` 返回：

```ts
{ reviewed: 12, exported: 10, skippedMissingAiError: 2 }
```

点击“导出评测样本”后断言成功消息包含“已导出 10 条”，警告消息包含“另有 2 条历史样本因缺少模型原始错因未导出”。错误场景断言后端消息能显示，且按钮忙碌状态会恢复。

- [ ] **Step 5: 抽取安全 Blob 保存函数并新增评测下载函数**

```ts
export interface EvaluationDownloadSummary {
  reviewed: number
  exported: number
  skippedMissingAiError: number
}

function dispositionFilename(disposition: unknown, fallback: string): string {
  const matched = typeof disposition === 'string'
    ? /filename="([^"]+)"/.exec(disposition)
    : null
  return matched?.[1] ?? fallback
}

function saveBlob(blob: Blob, filename: string): void {
  const url = URL.createObjectURL(blob)
  try {
    const link = document.createElement('a')
    link.href = url
    link.download = filename
    document.body.appendChild(link)
    link.click()
    link.remove()
  } finally {
    URL.revokeObjectURL(url)
  }
}

export async function downloadEvaluationCases(
  assignmentId: number,
): Promise<EvaluationDownloadSummary> {
  const response = await api.get<Blob>(
    `/evaluation/assignments/${assignmentId}/grading-cases.csv`,
    { responseType: 'blob' },
  )
  saveBlob(response.data, dispositionFilename(
    response.headers['content-disposition'], `grading-cases-${assignmentId}.csv`,
  ))
  return {
    reviewed: Number(response.headers['x-evaluation-reviewed'] ?? 0),
    exported: Number(response.headers['x-evaluation-exported'] ?? 0),
    skippedMissingAiError: Number(
      response.headers['x-evaluation-skipped-missing-ai-error'] ?? 0,
    ),
  }
}
```

`downloadExerciseDocx` 与新函数共同使用 `saveBlob(...)`；令牌仍只放在 Authorization 头，绝不拼到 URL。

- [ ] **Step 6: 在作业卡片加入教师可见入口**

在 `AssignmentView.vue` 的操作区增加次级按钮：

```vue
<button
  class="secondary-button"
  :disabled="busyId === item.id"
  @click="exportEvaluation(item)"
>
  导出评测样本
</button>
```

导出 0 条时提示“这份作业还没有已复核的 AI 样本”；剔除数大于 0 时必须额外警告，不能只报成功。

- [ ] **Step 7: 运行两端针对性与全量验证**

Run:

```powershell
mvn -f backend/pom.xml -Dtest=EvaluationExportApiTest test
npm --prefix frontend run test -- src/views/AssignmentView.test.ts src/api/client.test.ts
mvn -f backend/pom.xml test
npm --prefix frontend run test
npm --prefix frontend run typecheck
npm --prefix frontend run build
```

Expected: 所有检查通过；下载文件名为 `grading-cases-{id}.csv`；跳过计数在页面可见。

- [ ] **Step 8: 提交一键导出链路**

```powershell
git add backend/src/main/java/com/homework/analysis/evaluation/EvaluationExport.java `
  backend/src/main/java/com/homework/analysis/evaluation/EvaluationExportService.java `
  backend/src/main/java/com/homework/analysis/evaluation/EvaluationExportController.java `
  backend/src/test/java/com/homework/analysis/evaluation/EvaluationExportApiTest.java `
  frontend/src/api/client.ts frontend/src/api/client.test.ts `
  frontend/src/views/AssignmentView.vue frontend/src/views/AssignmentView.test.ts `
  frontend/src/components.d.ts
git commit -m "feat: download auditable evaluation samples"
```

### Task 5: 清理已知交付噪音

**Files:**
- Modify: `backend/pom.xml`
- Delete: `backend/src/main/java/com/homework/analysis/classroom/OwnershipGuard.java`
- Delete: `backend/src/test/java/com/homework/analysis/classroom/OwnershipGuardTest.java`
- Create: `frontend/public/favicon.svg`
- Modify: `frontend/index.html`

**Interfaces:**
- Preserves: 正式数据库仍是 MySQL 8.4；H2 只用于测试与 demo。
- Produces: 测试/演示使用 H2 `2.3.232`，与当前 Flyway 验证上限一致。
- Removes: 无任何生产调用方的 `OwnershipGuard`。

- [ ] **Step 1: 用只读命令再次确认死代码范围**

Run:

```powershell
rg -n "OwnershipGuard" backend/src
```

Expected: 只有 `OwnershipGuard.java` 与 `OwnershipGuardTest.java` 两处；若出现生产调用方，停止删除并改为评估复用。

- [ ] **Step 2: 删除死代码及其孤立测试**

用补丁删除两个文件。不要改动各 Service 已有的 `requireOwned(...)` 查询，因为那些查询通过数据库条件隐藏跨教师资源，语义比内存 owner-id 比较更完整。

- [ ] **Step 3: 固定 H2 版本并验证告警消失**

在 `pom.xml` 属性增加：

```xml
<h2.version>2.3.232</h2.version>
```

Run: `mvn -f backend/pom.xml -Dtest=HomeworkAnalysisApplicationTest test`

Expected: PASS；日志显示 `H2 2.3.232`，不再出现“newer than the version Flyway has been verified with”。

- [ ] **Step 4: 增加 SVG favicon 并声明资源**

`frontend/public/favicon.svg` 使用静态、无脚本 SVG：白色作业纸、蓝色边框和红色批改勾。`frontend/index.html` 增加：

```html
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
```

禁止外链字体、脚本和图片。

- [ ] **Step 5: 运行验证并检查构建产物**

Run:

```powershell
mvn -f backend/pom.xml test
npm --prefix frontend run build
Test-Path frontend/dist/favicon.svg
rg -n "OwnershipGuard" backend/src
```

Expected: 后端全量通过；前端构建通过；`Test-Path` 返回 `True`；最后一条 `rg` 无结果。

- [ ] **Step 6: 提交交付噪音清理**

```powershell
git add backend/pom.xml backend/src/main/java/com/homework/analysis/classroom/OwnershipGuard.java `
  backend/src/test/java/com/homework/analysis/classroom/OwnershipGuardTest.java `
  frontend/public/favicon.svg frontend/index.html
git commit -m "chore: remove known delivery warnings"
```

### Task 6: 冻结正式论文实验协议

**Files:**
- Create: `docs/experiments/2026-09-15-论文正式实验方案.md`
- Modify: `docs/experiments/2026-09-14-实验记录模板.md`
- Modify: `evaluation/README.md`

**Interfaces:**
- Consumes: Task 3 的 CSV v2 与指标 JSON。
- Produces: 一份执行前冻结的研究方案，不包含实验结果和身份信息。
- Produces: 仓库外归档目录 `E:\homework-analysis-experiment-archive\2026-09-main-study`。

- [ ] **Step 1: 写清三个可证伪研究问题**

方案固定为：

1. RQ1：在 100–300 条真实、匿名的初中数学解答题作答上，AI 建议分与教师最终分的 MAE、完全一致率和 1 分容差命中率是多少？
2. RQ2：七类错因上的宏平均 F1、逐类 F1 和主要混淆方向是什么？
3. RQ3：教师采纳、修改、驳回比例分别是多少，教师与模型平均/中位耗时差异如何？

不预写目标数值，不把“系统有效”写成先验结论。

- [ ] **Step 2: 固定样本纳入与排除规则**

纳入：真实班级、解答题、模型任务成功、教师已复核、模型原始错因存在。排除：演示数据、客观题规则评分、待复核项、身份字段未完成脱敏的文件、缺失 `ai_error_type` 的 V7 前历史行。用时或令牌单列缺失不删除整行，只在对应统计量中排除并记录缺失数。

- [ ] **Step 3: 固定人工评分与偏差控制**

至少一名合作教师依据题库评分项独立给出最终分与最终错因；教师不可在录入人工真值前看到汇总指标。若只有一名教师，论文必须把单评偏差列为限制；若有两名教师，先独立评分，再记录分歧裁决，不用模型输出参与裁决。

- [ ] **Step 4: 固定归档布局与哈希命令**

```text
E:\homework-analysis-experiment-archive\2026-09-main-study\
  protocol\论文正式实验方案.md
  input\grading-cases.csv
  input\error-labels.csv
  output\metrics.json
  logs\run.txt
  checksums\sha256.txt
```

哈希命令：

```powershell
$experimentRoot = 'E:\homework-analysis-experiment-archive\2026-09-main-study'
Get-FileHash "$experimentRoot\input\grading-cases.csv" -Algorithm SHA256
Get-FileHash "$experimentRoot\input\error-labels.csv" -Algorithm SHA256
Get-FileHash "$experimentRoot\output\metrics.json" -Algorithm SHA256
```

- [ ] **Step 5: 更新实验记录模板**

模板增加 `ACCEPT/MODIFY/REJECT` 三项比例、模型名集合、提示词版本集合、导出总数、实际导出数、缺失原始错因剔除数、各可选列缺失数。明确 `teacher_modification_rate = modify + reject` 是一致性检查，不是第四种决策。

- [ ] **Step 6: 审查文档中没有真实身份信息或虚构数字**

Run:

```powershell
rg -n "张三|李四|七年级一班|MathDemo|CASE-001|0\.96|0\.375" `
  docs/experiments/2026-09-15-论文正式实验方案.md
```

Expected: 无结果。示例账号、示例样本和历史演示指标不得出现在正式方案中。

- [ ] **Step 7: 提交实验协议**

```powershell
git add docs/experiments/2026-09-15-论文正式实验方案.md `
  docs/experiments/2026-09-14-实验记录模板.md evaluation/README.md
git commit -m "docs: freeze formal evaluation protocol"
```

### Task 7: 执行正式实验与答辩级验收

**Files:**
- Modify: `README.md`
- Modify: `docs/2026-09-14-项目阶段总结.md`
- Do not add: `E:\homework-analysis-experiment-archive\2026-09-main-study\**`

**Interfaces:**
- Consumes: 合作教师已授权使用的真实作答、已配置的真实模型和 Task 6 协议。
- Produces: 仓库外匿名 CSV、指标 JSON、运行日志和 SHA-256；仓库内只记录汇总结论、样本量、方法和限制。

- [ ] **Step 1: 在任何真实外部操作前执行安全门禁**

向用户展示：目标作业 ID、预计 AI 调用条数、所用模型、预估费用、持久化 MySQL 备份路径、CSV 输出路径和回滚方式。只有用户明确确认后才继续；确认前只允许使用 demo/H2 或一次性 Testcontainers。

- [ ] **Step 2: 创建仓库外受控归档目录**

```powershell
$experimentRoot = 'E:\homework-analysis-experiment-archive\2026-09-main-study'
New-Item -ItemType Directory -Force `
  "$experimentRoot\protocol", "$experimentRoot\input", `
  "$experimentRoot\output", "$experimentRoot\logs", "$experimentRoot\checksums"
Copy-Item 'docs\experiments\2026-09-15-论文正式实验方案.md' `
  "$experimentRoot\protocol\论文正式实验方案.md"
Copy-Item 'evaluation\data\error_labels.example.csv' `
  "$experimentRoot\input\error-labels.csv"
```

- [ ] **Step 3: 通过教师端导出真实匿名 CSV 并人工复查**

在作业管理页点击“导出评测样本”，把文件保存为 `$experimentRoot\input\grading-cases.csv`。记录页面提示的 reviewed、exported、skipped 三个计数。人工打开表头，确认只包含计划定义的 14 列；用以下检查阻止明显身份字段进入归档：

```powershell
$csvPath = 'E:\homework-analysis-experiment-archive\2026-09-main-study\input\grading-cases.csv'
$header = Get-Content $csvPath -TotalCount 1
$header
(Import-Csv $csvPath | Measure-Object).Count
```

Expected: 表头与 CSV v2 完全一致；行数等于页面 `exported`；没有姓名、学号、班级、学校列。

- [ ] **Step 4: 运行冻结评测并保存完整控制台日志**

```powershell
$experimentRoot = 'E:\homework-analysis-experiment-archive\2026-09-main-study'
python evaluation/evaluate_grading.py `
  --grading "$experimentRoot\input\grading-cases.csv" `
  --labels "$experimentRoot\input\error-labels.csv" `
  --output "$experimentRoot\output\metrics.json" *>&1 | `
  Tee-Object "$experimentRoot\logs\run.txt"
if ($LASTEXITCODE -ne 0) { throw "正式评测失败，退出码 $LASTEXITCODE" }
```

Expected: 退出码 0；样本数在协议规定的 100–300 范围；`unknown_labels` 为空；`teacher_modification_rate` 等于 `modify + reject`。

- [ ] **Step 5: 生成归档哈希并复制到实验记录**

```powershell
$experimentRoot = 'E:\homework-analysis-experiment-archive\2026-09-main-study'
Get-ChildItem "$experimentRoot\input", "$experimentRoot\output", "$experimentRoot\protocol" -File | `
  Get-FileHash -Algorithm SHA256 | Format-Table -AutoSize | `
  Out-File "$experimentRoot\checksums\sha256.txt" -Encoding utf8
```

在实验记录中只记录哈希、样本数量、聚合指标、实验时间、模型名和提示词版本；不复制任何逐学生明细到仓库。

- [ ] **Step 6: 运行完整自动化验证**

Run:

```powershell
mvn -f backend/pom.xml test
mvn -f backend/pom.xml verify -Pmysql-it
npm --prefix frontend run test
npm --prefix frontend run typecheck
npm --prefix frontend run build
python -m unittest discover -s evaluation/tests -v
```

Expected: 全部命令退出码 0；MySQL 集成测试使用一次性 `mysql:8.4` 容器，不触碰持久化卷。

- [ ] **Step 7: 运行浏览器主流程验收**

终端一：

```powershell
mvn -f backend/pom.xml spring-boot:run "-Dspring-boot.run.profiles=demo"
```

终端二：

```powershell
npm --prefix frontend run dev
```

终端三：

```powershell
npm --prefix frontend run test:e2e
```

Expected: 登录 → 题库公式 → 作业 → 复核 → 画像 → 分层练习 → Word 导出全链路通过；额外人工确认复核页最终错因只能选择固定标签，作业页能下载评测 CSV，浏览器不再请求 `/favicon.ico`。

- [ ] **Step 8: 更新 README 与阶段总结**

只记录本轮实际运行的测试数量、实验样本量、聚合指标、未验证项和限制。阶段总结追加“第三阶段完成情况”，不改写第八至十节的历史快照。若真实实验尚未达到 100 条，明确写“试运行”，不得写成正式实验完成。

- [ ] **Step 9: 最终审查工作树与秘密/数据泄露**

Run:

```powershell
git diff --check
git diff --stat
git status --short
git ls-files | rg "grading-cases|metrics\.json|experiment-archive"
rg -n "MODEL_API_KEY=.+|JWT_SECRET=.+|Bearer [A-Za-z0-9_-]+" `
  README.md docs evaluation backend frontend -g '!target' -g '!dist' -g '!node_modules'
```

Expected: `git diff --check` 无错误；真实 CSV/JSON/归档路径没有进入 Git；没有令牌、密钥或真实身份信息；改动只覆盖本计划文件。

- [ ] **Step 10: 提交交付文档**

```powershell
git add README.md docs/2026-09-14-项目阶段总结.md
git commit -m "docs: record final experiment and acceptance"
```

## Deferred Work

- Word OMML 可编辑公式、Markdown、录入时 LaTeX 严格校验不影响本阶段论文实验，继续延期。
- 完整跨域前后端分离部署不在本计划内；本阶段只暴露下载所需响应头，正式部署仍建议同源或反向代理。
- 学生端、家长端、管理员端、OCR、在线考试、RAG、向量数据库继续作为明确非目标。
- `OpenAiCompatibleModelClient` 的 JDK 弃用 API 告警单独做一次依赖/API 调查后再处理，不能在没有替代契约测试的情况下机械改写。

## Plan Self-Review

- **Spec coverage:** 原设计 7.5 的教师复核由 Tasks 1–2 收口；13.2 的评分误差、错因、采纳/修改/驳回、用时与模型对比由 Tasks 3、6、7 覆盖；14 的追溯与隐私要求由模型/提示词溯源字段、计数头和仓库外归档覆盖。
- **Scope control:** 没有数据库迁移、微服务、新角色、学生端、OCR、RAG、Markdown 或 OMML；公式实时预览也不与实验收口混在本计划中。
- **Type consistency:** 后端 `ErrorType.name()`、前端 `ErrorType`、示例标签表和 CSV 都使用同一组大写编码；`teacher_modified` 与 `review_decision` 有显式一致性校验。
- **Data safety:** 真实数据路径固定在仓库外；导出接口不含身份字段；执行真实模型和持久化 MySQL 前有明确确认门禁。
- **Fresh baseline:** 计划编写日 2026-09-15 实测后端 120 个用例、前端 46 个用例、Python 20 个用例通过，前端类型检查和生产构建通过；MySQL IT 与 Playwright 本轮未重跑，所以没有把历史结果冒充本轮证据。
- **Placeholder scan:** 任务没有使用待补实现、泛化错误处理或未定义接口；所有实现步骤给出了具体文件、签名、字段、命令和期望结果。
